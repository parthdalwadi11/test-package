{
    "name": "pmd/laravel-installer-package",
    "description": "A package to install Laravel",
    "type": "project",
    "license": "MIT",
    "authors": [
        {
            "name": "Your Name",
            "email": "your.email@example.com"
        }
    ],
    "minimum-stability": "stable",
    "prefer-stable": true,
    "require": {
        "php": "^7.3|^8.0",
        "laravel/laravel": "^10.0",
        "pmd/laravel-installer-package": "dev-master"
    },
    "scripts": {
        "post-root-package-install": [
            "@php -r \"file_exists('.env') || copy('.env.example', '.env');\"",
            "@php artisan key:generate"
        ],
        "post-create-project-cmd": [
            "@php artisan migrate"
        ]
    }
}